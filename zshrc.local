# Set word style to bash
autoload -U select-word-style
select-word-style bash

# Set PATH order
export PATH="$HOME/bin:$PATH"

# Go configuration
export GOPATH="$HOME"

# GPG2 configuration
alias gpg="gpg2"
export GPG_TTY=$(tty)

alias curl=safe-curl
