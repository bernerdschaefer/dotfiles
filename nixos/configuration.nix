# Edit this configuration file to define what should be installed on
# your system.  Help is available in the configuration.nix(5) man page
# and in the NixOS manual (accessible by running ‘nixos-help’).

{ config, pkgs, ... }:

{
  imports =
    [ # Include the results of the hardware scan.
      ./hardware-configuration.nix
    ];

  # Use the GRUB 2 boot loader.
  boot.loader.grub.enable = true;
  boot.loader.grub.version = 2;
  # Define on which hard drive you want to install Grub.
  boot.loader.grub.device = "/dev/sda";

  # disable journal checking in virtualbox environment
  boot.initrd.checkJournalingFS = false;

  # enable sshd
  services.openssh.enable = true;

  # enable virtualbox guest additions
  services.virtualbox.enable = true;

  environment.systemPackages = with pkgs; [
    git
    vim
  ];

  # passwordless sudo for wheel users
  security.sudo.wheelNeedsPassword = false;

  # ssh -p2222 -i ~/.ssh/id_vagrant localhost
  users.extraUsers.bernerd =
    { createHome = true;
      home = "/home/bernerd";
      description = "Bernerd";
      extraGroups = [ "wheel" ];
      useDefaultShell = true;
      openssh.authorizedKeys.keys = [ "ssh-rsa  vagrant insecure public key" ];
    };
}
